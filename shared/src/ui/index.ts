// Universal UI primitives — work on both web (via react-native-web) and
// native (Expo / React Native). Both platforms accept className strings
// (web: Tailwind CSS; native: NativeWind v4 jsx transform).
export { Button } from './Button';
export { Input } from './Input';
